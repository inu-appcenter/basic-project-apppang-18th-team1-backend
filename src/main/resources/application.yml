spring:
  application:
    name: appang
  datasource:
    url: jdbc:mysql://localhost:3307/appang
    username: appang
    password: appangpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true